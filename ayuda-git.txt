git init
git statu
git add
git commit -m

[primera vez usando el push]
crear repositorio en github y copiar el remote
y pegar en el bash OJO con el main o master

despues de esto se hace el push normalmente

git push

